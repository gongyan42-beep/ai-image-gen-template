# AI图片生成网站部署模板 - 使用说明

## 🎉 模板已创建完成!

位置: `/Users/gusuping/ai-image-gen-template/`

打包文件: `/Users/gusuping/ai-image-gen-template-v1.0.tar.gz` (12KB)

---

## 📦 模板包含内容

### 核心文件
- `src/` - TypeScript 源代码
  - `gemini.ts` - Nano Banana API 集成 + 图片压缩
  - `server.ts` - Express 服务器
  - `types.ts` - TypeScript 类型定义
- `public/index.html` - 前端页面(中文UI)
- `package.json` - 项目依赖配置
- `tsconfig.json` - TypeScript 配置

### 部署文件
- `deploy.sh` - 一键部署脚本 (可执行)
- `.env.example` - 环境变量模板

### 文档
- `README.md` - 完整文档(英文)
- `QUICKSTART.md` - 快速开始指南
- `使用说明.md` - 本文件

### 其他
- `.gitignore` - Git 忽略规则

---

## 🚀 5分钟部署新项目

### 1. 解压模板
```bash
cd ~
tar -xzf ai-image-gen-template-v1.0.tar.gz
cp -r ai-image-gen-template/ 新项目名称/
cd 新项目名称/
```

### 2. 配置 API KEY
```bash
cp .env.example .env
nano .env  # 编辑配置
```

填入:
```
API_KEY=你的Nano_Banana_API密钥
PORT=3013  # 选择一个未占用的端口
```

### 3. 自定义项目

**必须修改** - `public/index.html`:
```html
Line 6:   <title>你的项目名 - AI图片生成</title>
Line 215: <h1>🎨 你的品牌名</h1>
Line 216: <p class="subtitle">你的项目描述</p>
```

**可选修改** - `src/gemini.ts` (Line 24-62):
- 修改 AI 提示词以适应你的具体需求
- 调整图片生成的风格和要求

### 4. 一键部署
```bash
chmod +x deploy.sh
./deploy.sh <域名> <端口号>

# 例如:
./deploy.sh newproject.longgonghuohuo.com 3013
```

### 5. 配置 Nginx
复制脚本输出的 Nginx 配置到宝塔面板。

---

## 🎯 下次部署更快!

你已经有了模板,下次只需要:

1. 复制模板文件夹
2. 修改 3 个地方:
   - `.env` (API_KEY + PORT)
   - `index.html` (品牌名称)
   - `gemini.ts` (AI提示词)
3. 运行 `./deploy.sh <域名> <端口>`

**预计时间**: 5-10分钟

---

## 📋 模板特点

✅ **自动图片压缩** - 1024px, 75%质量,避免413错误
✅ **TypeScript** - 类型安全
✅ **一键部署脚本** - 自动化部署流程
✅ **完整文档** - README + 快速开始指南
✅ **错误处理** - 完善的异常处理
✅ **日志记录** - 详细的运行日志

---

## 🔧 常见自定义

### 修改压缩参数
`src/gemini.ts` (Line 17-21):
```typescript
.resize(800, 800)     // 降低分辨率
.jpeg({ quality: 60 }) // 降低质量
```

### 修改 UI 颜色
`public/index.html` (Line 16):
```css
background: linear-gradient(135deg, #你的颜色1 0%, #你的颜色2 100%);
```

### 添加新配置项
1. `src/types.ts` - 添加类型
2. `public/index.html` - 添加表单
3. `src/server.ts` - 接收参数
4. `src/gemini.ts` - 使用参数

---

## 📞 快速命令参考

### 本地测试
```bash
npm install
npm run build
npm start
```

### 查看日志
```bash
ssh root@服务器IP 'tail -f /tmp/项目名.log'
```

### 重启服务
```bash
ssh root@服务器IP 'killall node && cd /www/wwwroot/域名 && nohup node dist/server.js > /tmp/项目名.log 2>&1 &'
```

---

## 💡 提示

1. **端口规划**: 建议为每个项目分配固定端口(3012, 3013, 3014...)
2. **备份模板**: 保留原始模板文件夹,不要在原模板上修改
3. **Git管理**: 可以为每个新项目初始化 git 仓库
4. **API KEY**: 所有项目可以共用同一个 Nano Banana API KEY

---

## 📖 更多信息

详细文档请查看:
- `README.md` - 完整技术文档
- `QUICKSTART.md` - 快速开始指南
- `deploy.sh` - 部署脚本(带详细注释)

---

**模板版本**: v1.0.0  
**创建日期**: 2024-12-10  
**作者**: AI Assistant  
**基于项目**: aihuantu-project
